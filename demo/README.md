# Demo

Run:

```bash
python -m thesis_revision_professor demo
```

The command copies the fake thesis sample into `demo/input/` and generates review outputs in `demo/output/`.

All demo content is synthetic and does not contain real thesis text.
